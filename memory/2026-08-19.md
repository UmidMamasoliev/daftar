# 2026-08-19

Nima oʻzgardi: **3.9 syomkasi tugagani tasdiqlandi** — odam darsda Vercel qismini
koʻrsatmaganini aytdi va hammasini off-screen qilishni buyurdi. Odam koʻrsatmasi bilan bosh
agent **GitHub ↔ Vercel ulanishini** yopdi: Vercel GitHub App brauzer orqali
`UmidMamasoliev` hisobiga oʻrnatildi — «Only select repositories», faqat `daftar` reposi
(eng kam ruxsat), soʻng `vercel git connect` → Connected. 2026-08-18 dagi 3 kommit push
qilindi (`ef5f1fa..0b0634c`) va push **avtomatik production deploy** yaratdi
(`daftar-3mvpqoo18`, source: git, commit `0b0634c`) — zanjir jonli tasdiqlandi;
https://daftarim.vercel.app 200 bilan yangi holatni beradi. Endi har `master` push =
production deploy.

Qayerda toʻxtadik: **3.10 (dashboard, Spec Kit bilan) boshlashga tayyor** — `specify-cli`
oʻrnatilgan. 2026-08-18 dagi qoldiqlar roʻyxati kuchda qoladi.

**MUHIM shart — odamning aniq koʻrsatmasi, 3.10 boshlanishidan OLDIN bajariladi:** 3.10
`specify init` bilan boshlanmaydi. Avval mavjud ish tartibimiz Spec Kit bilan solishtiriladi —
talaba (dars tomoshabini) ikkalasini ham tushunishi uchun. Markaziy savol — **«jamoa» farqi**:
Spec Kit'da bizniki kabi alohida agentlar jamoasi YOʻQ; u yerda bitta AI assistant bor, uni
bosqich-buyruqlar, shablonlar va skriptlar boshqaradi. Bizda esa aniq rollarga boʻlingan
agentlar jamoasi: manager taqsimlaydi, pm/dizayn/backend/frontend/qa/hujjat oʻz ishini qiladi.
Solishtiruv uch qismdan iborat:
1. **Oqim:** bizda discovery → decisions → prds spec → design → TDD qurish → qa ↔ Spec Kit'da
   constitution → specify → clarify → plan → tasks → implement.
2. **Rollar («jamoa»):** bizdagi agentlar (pm, dizayn, backend, frontend, hisobot, qa, hujjat,
   manager) ↔ Spec Kit'da rol taqsimoti — kim ishni qiladi va nima uni boshqaradi (bir assistant
   + buyruqlar/shablonlar/skriptlar, roʻyxatga boʻlingan agentlar emas).
3. **Papkalar:** bizda `discovery/`, `decisions/`, `prds/`, `design/`, `memory/`, `lessons/` ↔
   Spec Kit'da `.specify/` va `specs/<feature>/` ichidagi `spec.md`, `plan.md`, `tasks.md` va h.k.

Ikkinchi urgʻu — **feature qanday quriladi**: Spec Kit'da `specs/<feature>/` ichida
spec.md → plan.md → tasks.md, soʻng tasks roʻyxatidan birma-bir implement; bizda esa prds spec +
design → backend/frontend TDD sikli → qa tekshiruvi. Talaba ikkala usul bir xil maqsadga qanday
borishini koʻrishi kerak.

Bu solishtiruv — dars materiali. Qaysi qismi off-screen tayyorlanadi, qaysi qismi kamerada
koʻrsatiladi — buni odam yangi sessiyada hal qiladi (hozir hal qilinmagan).

Kutilmagan narsalar: (1) Vercel'da avvaldan BOSHQA GitHub hisobi (`ahsanlabsdev`) ulangan
ekan — endi ikkala ulanish turibdi, `daftar` faqat `UmidMamasoliev` ulanishidan foydalanadi.
(2) GitHub ogohlantirishi: **2026-09-05 dan 2FA majburiy** — buni odam oʻzi yoqishi kerak;
agent xavfsizlik sozlamalariga tegmadi (Vercel'dagi 2FA taklifi ham faqat yopildi).
(3) CLI `vercel git connect` GitHub App oʻrnatilmaguncha ishlamadi — oʻrnatish faqat
brauzer oqimi orqali boʻladi.

## Ikkinchi sessiya — 3.10: dashboard GitHub Spec Kit bilan

Nima oʻzgardi: **3.10 bajarildi — dashboard qurildi, birinchi marta GitHub Spec Kit oqimi
bilan.** Yuqoridagi odam sharti bajarildi: avval **solishtiruv** berildi (oqim, rollar
(«jamoa»), papkalar — dars materiali sifatida chatda, faylga yozilmadi), soʻng Spec Kit
oʻrnatildi: `specify init --here --integration claude` → `.specify/` (constitution,
shablonlar, skriptlar) va `.claude/skills/speckit-*` (10 skill). Eslatma: bu versiyada
(`specify-cli 0.16.5`) feature uchun branch ochish ixtiyoriy git-extension — biz uni
oʻrnatmadik, hamma ish `master` da ketdi.

Spec Kit oqimi toʻliq oʻtildi: constitution v1.0.0 (`.specify/memory/constitution.md`,
AGENTS.md + `decisions/` asosida) → specify (`specs/001-dashboard/spec.md` — 5 user story,
14 FR; manba `prds/dashboard.md`) → clarify (bitta savol — navigatsiya bandi; odam **B** ni
tanladi: «Yozuv» bandi navigatsiyadan olib tashlanadi) → plan (`plan.md`, `research.md`,
`data-model.md`, `quickstart.md`) → tasks (21 vazifa) → analyze (0 CRITICAL; «oxirgi yozuvlar
tartibi» talqini 0067 ga yozildi) → implement.

**Implement sof Spec Kit rejimida oʻtdi — kodni bosh agent oʻzi yozdi**, odamning aniq
ruxsati bilan: «bosh agent kod yozmaydi» qoidasiga shu feature uchun istisno berildi (usulni
darsda koʻrsatish uchun). TDD saqlandi — har qadam qizil testdan boshlandi.

Natija: dashboard endi **bosh sahifa** (sarlavha «Daftar»). Yangi fayllar:
`platform/src/ui/Dashboard.tsx`, `platform/src/domain/dashboard.ts` (`oyYigindilari`,
`zaxiraEslatmasiKerakmi`), `platform/src/ui/KursSorov.tsx` (Hisobot'dan ajratildi — bitta kurs
soʻrash bloki endi ikki ekranda ishlaydi). Navigatsiya (0067): **Bosh, Yozuvlar, Qarz daftari,
Hisobot, Zaxira**; «＋ Yozuv» tugmasi bosh sahifada, forma oʻzi ochilgan ekranga qaytadi.
Testlar 1023 → **1066 Vitest (3× barqaror)**, Playwright 11 → **12** (yangi dashboard e2e),
`tsc` va `build` toza.

`qa` agenti mustaqil tekshirdi — **Spec Kit'dan tashqarida, bizning yoʻl darvozasi bilan**:
25/25 mezon PASS, 4 past topilma, hammasi yopildi: (1) `KursSorov` «Saqlash» da in-flight
himoya yoʻq edi — 2026-08-18 qoidasining qoʻllanishi, test bilan yopildi (1066-test);
(2) `index.css` da eski 0063 izohlari; (3) `spec.md` da band nomi «Dashboard» ↔ «Bosh»
nomuvofiqligi; (4) `plan.md` da krill harflar.

Qaror: `decisions/0067-dashboard-navigatsiya.md` (nav **B** — odam tanladi; «Bosh»/«Daftar»
nomlari, roʻyxat 5 ta, eslatma bosilmaydi — bosh agent tanlovi, faylda qayd etilgan).
Hujjatlar: `platform/KELISHUV.md` 27-boʻlim, `design/uslub.md` navigatsiya boʻlimi
(«VAQTINCHALIK» belgisi olib tashlandi), `prds/dashboard.md` yangilandi, `TEKSHIRUV.md`
(1066/12 kutilmalari), `AGENTS.md` («0067 gacha»; 0063 vaqtinchalik bandi almashtirildi).

Deploy: `master` ga push = avtomatik production deploy (https://daftarim.vercel.app) —
kommit va push shu yozuvdan keyin bosh agent tomonidan qilinadi.

Qayerda toʻxtadik: **Daftar toʻliq.** Beshala qism — kirim-chiqim yozuvlari, qarz daftari,
oylik hisobot, zaxira, dashboard — qurilgan va testli; 0022 tayyorlik mezoni bajarildi
(dashboard oxirgi qism edi). 3.10 syomkasining yakuniy xulosa qismi qoldi — uni odam
kamerada yopadi.

Kutilmagan narsalar: (1) `clarify` haqiqiy ochiq savolni topdi — aynan 0063 ataylab ochiq
qoldirgan navigatsiya; boshqa hech narsa chiqmadi, chunki eski discovery → decisions oqimi
specni allaqachon yopib qoʻygan edi. (2) Spec Kit'da alohida tekshiruvchi rol yoʻq — `qa`
bizning yoʻldan qoʻshilgani 4 real topilma berdi. (3) «Oxirgi kurs» va xavfsiz jami kabi
tayyor domain qoidalari tufayli implement asosan ekran qatlami ishi boʻlib chiqdi.
